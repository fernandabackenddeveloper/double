{
  "name": "double",
  "version": "0.0.1",
  "main": "server.ts",
  "scripts": {
    "start": "node ./dist/server.js",
    "dev": "ts-node-dev --transpile-only --ignore-watch node_modules src/index.ts",
    "dev:analysis": "ts-node-dev --transpile-only --ignore-watch node_modules src/analyse.ts",
    "build": "tsc --build",
    "clean": "tsc --build --clean"
  },
  "devDependencies": {
    "ts-node-dev": "^2.0.0",
    "typescript": "^4.7.3"
  },
  "dependencies": {
    "axios": "^0.27.2"
  }
}
